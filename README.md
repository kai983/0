# 지식 저장소

유튜브, 기사, 레포트, 글 등 흩어진 지식을 한 곳에 모으고, AI로 요약-구조화해서
언제든 꺼내 쓸 수 있게 정리하는 개인 지식 저장소 앱입니다.
Notion(깔끔한 페이지 UI)과 Readwise(하이라이트/카드 스타일)를 참고해 디자인했습니다.

## 구조

- `client/` - React(Vite) 오프라인 웹 앱. 데이터는 기기 로컬(localStorage)에만 저장되며 서버가 필요 없습니다.
- `client/android/` - Capacitor로 생성한 Android 프로젝트 (APK 빌드용)
- `.github/workflows/build-apk.yml` - GitHub Actions에서 자동으로 APK를 빌드하는 워크플로우

## 저장하는 법

유튜브나 브라우저에서 **공유 -> 지식 저장소** 를 고르면 제목과 URL이 채워진 채로 저장됩니다.
앱을 먼저 열 필요가 없습니다.

## AI 재가공 방식

두 가지 경로가 있습니다.

**자동 (기본)**: 설정할 것이 없습니다. 무료 키가 빌드 때 `GEMINI_KEY` 시크릿에서 주입되어
APK에 들어가므로, 설치 직후부터 공유한 콘텐츠가 클릭 없이 요약-태그까지 저장됩니다.
유튜브 링크는 Gemini가 영상을 직접 봅니다(1-2분 소요).
다른 키를 쓰고 싶으면 설정(팔레트 버튼)에 붙여넣으면 그 키가 우선하며, 그 키는 기기에만 저장됩니다.
설정의 "연결 테스트"로 지금 어떤 상태인지 확인할 수 있습니다.

**수동**: 키가 없으면 기존 방식 그대로, 구독 중인 Claude.ai 를 활용합니다.

1. 지식 카드에서 "AI 프롬프트 생성"을 누르면 요약용 프롬프트가 만들어집니다.
2. "AI 앱으로 보내기" 를 누르면 Claude, ChatGPT, 제미나이 등 설치된 앱이 프롬프트가 채워진 채로 열립니다.
3. 답변을 **공유 -> 지식 저장소** 로 보내면 원래 카드에 자동으로 붙습니다. 복사와 붙여넣기도 그대로 쓸 수 있습니다. 저장하면, 한 줄 요약-핵심 내용-인사이트-추천 태그가
   구조화되어 저장되고 태그가 자동으로 반영됩니다.

## 웹에서 실행

```bash
cd client
npm install
npm run dev      # http://localhost:5173
```

빌드된 정적 파일(`npm run build` 결과인 `client/dist/`)은 PWA로 동작합니다.
모바일 브라우저에서 열고 "홈 화면에 추가"를 하면 앱처럼 설치해 오프라인으로 쓸 수 있습니다.

## APK 빌드

이 리포지토리를 push하면 GitHub Actions(`build-apk.yml`)가 자동으로 실행되어
Android 디버그 APK를 빌드하고 워크플로우 아티팩트로 업로드합니다.
Actions 탭 → 해당 워크플로우 실행 → Artifacts에서 `knowledge-archive-debug-apk`를 내려받으면 됩니다.

로컬에 Android SDK(Android Studio)가 있다면 직접 빌드할 수도 있습니다.

```bash
cd client
npm install
npm run build
npx cap sync android
cd android
./gradlew assembleDebug
# 결과물: client/android/app/build/outputs/apk/debug/app-debug.apk
```

## 버전

`client/package.json` 의 `version` 이 기준이고, 빌드할 때 안드로이드 `versionName` 과
앱 화면(테마 시트 하단)에 자동으로 반영됩니다.

- 작은 변경(문구, 버그 수정, 다듬기): +0.01
- 큰 변경(화면 구조, 기능 추가): +0.1

| 버전 | 내용 |
|---|---|
| 0.1 | 오프라인 아카이빙 앱 첫 동작, APK 빌드 파이프라인 |
| 0.2 | 모바일 UI 재설계 (상단 앱바 + 하단 탭바, safe-area 대응) |
| 0.3 | 테마 4종(편집형/인덱스형/몰입형/토스풍), Pretendard 적용 |
| 0.31 | 구분 기호를 하이픈으로 통일 |
| 0.32 | 버전 표기 체계 도입 |
| 0.4 | 학습 기능 (간격 반복 문답 카드) |
| 0.5 | 안드로이드 공유로 저장, AI 앱으로 보내고 답변 공유로 받기 |
| 0.6 | Gemini 무료 API로 완전 자동 요약 (공유하면 클릭 없이 요약까지 저장) |
| 0.61 | 설정에 무료 키 원탭 생성 링크 |
| 0.62 | 무료 키 내장 (빌드 시 Secret 주입) + gemini-3.6-flash 전환 |
| 0.63 | 토스풍 기본 테마로, 어댑티브 아이콘 배경-전경 레이어 수정, 모노크롬 아이콘 추가, AI 키 오류 메시지에 원인 표시 |
| 0.64 | 키를 헤더-쿼리 양쪽 전송, 본문 직렬화 실패 시 자동 재시도, gemini-flash-latest 전환, 설정에 연결 테스트 추가 |
| 0.65 | 유튜브 공유 URL 정규화 - si 추적 파라미터 때문에 영상 요약이 400으로 실패하던 문제 |
| 0.66 | 요약의 볼드-기울임-코드 렌더링, 요약 후 다음 단계 안내, 영상 대기 시간 표시와 타임아웃 |
| 0.7 | 백업-복원 (JSON 파일 내보내기-가져오기), 설정에 현재 사용 중인 키 표시 |
| 0.71 | 앱이 켜져 있을 때 온 공유가 유실되던 문제 수정, 설정에 공유 상태 진단 추가 |

## 주요 기능

- 지식 등록: 텍스트 또는 URL(유튜브/기사/레포트 링크) + 메모 + 태그
- 사이드바 저장소: 저장 개수, 태그별 필터, 제목/본문/요약 검색
- AI 재가공: 프롬프트 생성 → Claude.ai 응답 붙여넣기 → 구조화된 요약 저장
- 학습: 지식마다 문답 카드를 만들고 간격 반복으로 복습 (다시 / 어려움 / 좋음 / 쉬움)
- 태그 편집, 카드 삭제
- 백업: 전체 데이터를 JSON 파일 하나로 내보내 공유 시트로 어디든 저장하고, 그 파일로 복원
- 오프라인 우선 (localStorage) + PWA 설치 지원
