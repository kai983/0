<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Kept in sync with the web UI's background and accent tokens so the
         native window and the WebView never disagree (e.g. during launch or
         over-scroll). -->
    <color name="appBackground">#FBFAF8</color>
    <color name="colorPrimary">#D9730D</color>
    <color name="colorPrimaryDark">#B35F0A</color>
    <color name="colorAccent">#D9730D</color>
</resources>
